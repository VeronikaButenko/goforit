
var a = prompt ('ведите число');
var b = prompt ('ведите cтепень');
var c = pow (a,b);

funktion pow (c,d) {
	if (minimal (c,d) || maximal (c,d)) {
    var result = e;
        for (var i=1; i<e; i++)
	};

return result;

alert (a*b);
console.log(a*b);


funktion minimal (c,d) < 1;
funktion maximal (c,d) > 1000;

// var userName = 'Вася';

// function showMessage() {
//   var message = 'Привет, я ' + userName;
//   alert(message);
// 

// showMessage(); // Привет, я Вася

// 	